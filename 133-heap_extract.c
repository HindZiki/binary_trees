#include "binary_trees.h"

/**
* heap_extract - function extracts root node of Max Binary Heap
* @root: double pointer to root node of heap
* Return: value stored in root node
*/

int heap_extract(heap_t **root)
{
	printf("%d", (*root)->n);
	return (0);
}